const Title = () => {
    return (
        <h1 className="font-bold">
            Gestione
        </h1> /* Loads the var from .env but I want this in the future to be logo or name */
    );
};

export default Title;
